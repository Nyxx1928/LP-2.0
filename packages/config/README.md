# Shared Config Package

This package stores shared presets consumed by apps and packages.

## Planned assets

- Base ESLint config
- Base Prettier config
- Base Tailwind preset
- Shared TypeScript presets
